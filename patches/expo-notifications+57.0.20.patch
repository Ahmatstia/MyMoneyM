diff --git a/node_modules/expo-notifications/build/DevicePushTokenAutoRegistration.fx.js b/node_modules/expo-notifications/build/DevicePushTokenAutoRegistration.fx.js
index 85cd97c..edf9531 100644
--- a/node_modules/expo-notifications/build/DevicePushTokenAutoRegistration.fx.js
+++ b/node_modules/expo-notifications/build/DevicePushTokenAutoRegistration.fx.js
@@ -1,4 +1,5 @@
 import 'abort-controller/polyfill';
+import { isRunningInExpoGo } from 'expo';
 import { UnavailabilityError } from 'expo-modules-core';
 import ServerRegistrationModule from './ServerRegistrationModule';
 import { addPushTokenListener } from './TokenEmitter';
@@ -72,7 +73,7 @@ export async function __handlePersistedRegistrationInfoAsync(registrationInfo) {
         console.warn('[expo-notifications] Error encountered while updating server registration with latest device push token.', e);
     }
 }
-if (ServerRegistrationModule.getRegistrationInfoAsync) {
+if (ServerRegistrationModule.getRegistrationInfoAsync && !isRunningInExpoGo()) {
     // A global scope (to get all the updates) device push token
     // subscription, never cleared.
     addPushTokenListener(async (token) => {
@@ -103,7 +104,7 @@ if (ServerRegistrationModule.getRegistrationInfoAsync) {
         console.error('[expo-notifications] Error reading persisted server registration info: ', e);
     });
 }
-else {
+else if (!isRunningInExpoGo()) {
     console.warn(`[expo-notifications] Error encountered while fetching auto-registration state, new tokens will not be automatically registered on server.`, new UnavailabilityError('ServerRegistrationModule', 'getRegistrationInfoAsync'));
 }
 //# sourceMappingURL=DevicePushTokenAutoRegistration.fx.js.map
\ No newline at end of file
diff --git a/node_modules/expo-notifications/build/PushTokenManager.native.js b/node_modules/expo-notifications/build/PushTokenManager.native.js
index 27bd573..d810717 100644
--- a/node_modules/expo-notifications/build/PushTokenManager.native.js
+++ b/node_modules/expo-notifications/build/PushTokenManager.native.js
@@ -1,3 +1,26 @@
-import { requireNativeModule } from 'expo-modules-core';
-export default requireNativeModule('ExpoPushTokenManager');
+import { requireOptionalNativeModule, requireNativeModule, Platform } from 'expo-modules-core';
+
+let nativeModule = null;
+try {
+  nativeModule = (typeof requireOptionalNativeModule === 'function' ? requireOptionalNativeModule('ExpoPushTokenManager') : null) || requireNativeModule('ExpoPushTokenManager');
+} catch {
+  let warningHasBeenShown = false;
+  nativeModule = {
+    addListener: () => {
+      if (!warningHasBeenShown) {
+        console.warn(`[expo-notifications] Listening to push token changes is not yet fully supported on ${Platform.OS}. Adding a listener will have no effect.`);
+        warningHasBeenShown = true;
+      }
+      return {
+        remove: () => { },
+      };
+    },
+    removeListener: () => { },
+    removeAllListeners: () => { },
+    emit: () => { },
+    listenerCount: () => 0,
+  };
+}
+
+export default nativeModule;
 //# sourceMappingURL=PushTokenManager.native.js.map
\ No newline at end of file
diff --git a/node_modules/expo-notifications/build/ServerRegistrationModule.native.js b/node_modules/expo-notifications/build/ServerRegistrationModule.native.js
index 359d085..ed75083 100644
--- a/node_modules/expo-notifications/build/ServerRegistrationModule.native.js
+++ b/node_modules/expo-notifications/build/ServerRegistrationModule.native.js
@@ -1,3 +1,14 @@
-import { requireNativeModule } from 'expo-modules-core';
-export default requireNativeModule('NotificationsServerRegistrationModule');
+import { requireOptionalNativeModule, requireNativeModule } from 'expo-modules-core';
+
+let nativeModule = null;
+try {
+  nativeModule = (typeof requireOptionalNativeModule === 'function' ? requireOptionalNativeModule('NotificationsServerRegistrationModule') : null) || requireNativeModule('NotificationsServerRegistrationModule');
+} catch {
+  nativeModule = {
+    addListener: () => { },
+    removeListeners: () => { },
+  };
+}
+
+export default nativeModule;
 //# sourceMappingURL=ServerRegistrationModule.native.js.map
\ No newline at end of file
diff --git a/node_modules/expo-notifications/build/TopicSubscriptionModule.android.js b/node_modules/expo-notifications/build/TopicSubscriptionModule.android.js
index 644a8bb..b9ece29 100644
--- a/node_modules/expo-notifications/build/TopicSubscriptionModule.android.js
+++ b/node_modules/expo-notifications/build/TopicSubscriptionModule.android.js
@@ -1,3 +1,16 @@
-import { requireNativeModule } from 'expo-modules-core';
-export default requireNativeModule('ExpoTopicSubscriptionModule');
+import { requireOptionalNativeModule, requireNativeModule } from 'expo-modules-core';
+
+let nativeModule = null;
+try {
+  nativeModule = (typeof requireOptionalNativeModule === 'function' ? requireOptionalNativeModule('ExpoTopicSubscriptionModule') : null) || requireNativeModule('ExpoTopicSubscriptionModule');
+} catch {
+  nativeModule = {
+    addListener: () => { },
+    removeListeners: () => { },
+    subscribeToTopicAsync: () => Promise.resolve(null),
+    unsubscribeFromTopicAsync: () => Promise.resolve(null),
+  };
+}
+
+export default nativeModule;
 //# sourceMappingURL=TopicSubscriptionModule.android.js.map
\ No newline at end of file
diff --git a/node_modules/expo-notifications/build/index.js b/node_modules/expo-notifications/build/index.js
index 3167134..f4e82cb 100644
--- a/node_modules/expo-notifications/build/index.js
+++ b/node_modules/expo-notifications/build/index.js
@@ -2,11 +2,7 @@ import { isRunningInExpoGo } from 'expo';
 function areWeTestingWithJest() {
     return process.env.JEST_WORKER_ID !== undefined || process.env.NODE_ENV?.toLowerCase() === 'test';
 }
-if (isRunningInExpoGo() && !areWeTestingWithJest()) {
-    const message = '`expo-notifications` functionality is not fully supported in Expo Go:\n' +
-        'We recommend you instead use a development build to avoid limitations. Learn more: https://expo.fyi/dev-client.';
-    console.warn(message);
-}
+// Expo Go limitation warning suppressed because MyMoney only uses local notifications
 export { getDevicePushTokenAsync } from './getDevicePushTokenAsync';
 export { unregisterForNotificationsAsync } from './unregisterForNotificationsAsync';
 export { getExpoPushTokenAsync } from './getExpoPushTokenAsync';
diff --git a/node_modules/expo-notifications/build/warnOfExpoGoPushUsage.js b/node_modules/expo-notifications/build/warnOfExpoGoPushUsage.js
index 8260405..61c4cfe 100644
--- a/node_modules/expo-notifications/build/warnOfExpoGoPushUsage.js
+++ b/node_modules/expo-notifications/build/warnOfExpoGoPushUsage.js
@@ -4,10 +4,7 @@ let didWarn = false;
 export const warnOfExpoGoPushUsage = () => {
     if (isRunningInExpoGo() && !didWarn) {
         const message = `expo-notifications: Android Push notifications (remote notifications) functionality provided by expo-notifications was removed from Expo Go with the release of SDK 53. Use a development build instead of Expo Go. Learn more at https://docs.expo.dev/develop/development-builds/introduction/.`;
-        if (Platform.OS === 'android') {
-            throw new Error(message);
-        }
-        else if (__DEV__) {
+        if (__DEV__) {
             didWarn = true;
             console.warn(message);
         }
diff --git a/node_modules/expo-notifications/src/DevicePushTokenAutoRegistration.fx.ts b/node_modules/expo-notifications/src/DevicePushTokenAutoRegistration.fx.ts
index 4150532..a7481ef 100644
--- a/node_modules/expo-notifications/src/DevicePushTokenAutoRegistration.fx.ts
+++ b/node_modules/expo-notifications/src/DevicePushTokenAutoRegistration.fx.ts
@@ -1,4 +1,5 @@
 import 'abort-controller/polyfill';
+import { isRunningInExpoGo } from 'expo';
 import { UnavailabilityError } from 'expo-modules-core';
 
 import ServerRegistrationModule from './ServerRegistrationModule';
@@ -98,7 +99,7 @@ export async function __handlePersistedRegistrationInfoAsync(
   }
 }
 
-if (ServerRegistrationModule.getRegistrationInfoAsync) {
+if (ServerRegistrationModule.getRegistrationInfoAsync && !isRunningInExpoGo()) {
   // A global scope (to get all the updates) device push token
   // subscription, never cleared.
   addPushTokenListener(async (token) => {
@@ -136,7 +137,7 @@ if (ServerRegistrationModule.getRegistrationInfoAsync) {
       console.error('[expo-notifications] Error reading persisted server registration info: ', e);
     }
   );
-} else {
+} else if (!isRunningInExpoGo()) {
   console.warn(
     `[expo-notifications] Error encountered while fetching auto-registration state, new tokens will not be automatically registered on server.`,
     new UnavailabilityError('ServerRegistrationModule', 'getRegistrationInfoAsync')
diff --git a/node_modules/expo-notifications/src/index.ts b/node_modules/expo-notifications/src/index.ts
index a93caaa..f50efa5 100644
--- a/node_modules/expo-notifications/src/index.ts
+++ b/node_modules/expo-notifications/src/index.ts
@@ -4,12 +4,7 @@ function areWeTestingWithJest() {
   return process.env.JEST_WORKER_ID !== undefined || process.env.NODE_ENV?.toLowerCase() === 'test';
 }
 
-if (isRunningInExpoGo() && !areWeTestingWithJest()) {
-  const message =
-    '`expo-notifications` functionality is not fully supported in Expo Go:\n' +
-    'We recommend you instead use a development build to avoid limitations. Learn more: https://expo.fyi/dev-client.';
-  console.warn(message);
-}
+// Expo Go limitation warning suppressed because MyMoney only uses local notifications
 
 export { getDevicePushTokenAsync } from './getDevicePushTokenAsync';
 export { unregisterForNotificationsAsync } from './unregisterForNotificationsAsync';
diff --git a/node_modules/expo-notifications/src/warnOfExpoGoPushUsage.ts b/node_modules/expo-notifications/src/warnOfExpoGoPushUsage.ts
index c6883b3..039d60d 100644
--- a/node_modules/expo-notifications/src/warnOfExpoGoPushUsage.ts
+++ b/node_modules/expo-notifications/src/warnOfExpoGoPushUsage.ts
@@ -7,9 +7,7 @@ export const warnOfExpoGoPushUsage = () => {
   if (isRunningInExpoGo() && !didWarn) {
     const message = `expo-notifications: Android Push notifications (remote notifications) functionality provided by expo-notifications was removed from Expo Go with the release of SDK 53. Use a development build instead of Expo Go. Learn more at https://docs.expo.dev/develop/development-builds/introduction/.`;
 
-    if (Platform.OS === 'android') {
-      throw new Error(message);
-    } else if (__DEV__) {
+    if (__DEV__) {
       didWarn = true;
       console.warn(message);
     }
diff --git a/node_modules/expo-notifications/tsconfig.json b/node_modules/expo-notifications/tsconfig.json
index f068839..de13b91 100644
--- a/node_modules/expo-notifications/tsconfig.json
+++ b/node_modules/expo-notifications/tsconfig.json
@@ -1,6 +1,6 @@
 // @generated by expo-module-scripts
 {
-  "extends": "expo-module-scripts/tsconfig.base",
+  "extends": "expo/tsconfig.base",
   "compilerOptions": {
     "rootDir": "./src",
     "outDir": "./build"
